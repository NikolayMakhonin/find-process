/* eslint-disable no-shadow */
import assert from 'assert'
import {waitProcessList, waitProcessTree} from './wait-process'

describe('wait-process', function () {
	this.timeout(1000)

	it('waitProcessList', async function () {
		// TODO
	})

	it('waitRepeat', async function () {
		// TODO
	})
})
