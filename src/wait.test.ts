/* eslint-disable no-shadow */
import assert from 'assert'
import {waitRepeat, withTimeout} from './wait'

describe('wait', function () {
	this.timeout(1000)

	it('withTimeout', async function () {
		// TODO
	})

	it('waitRepeat', async function () {
		// TODO
	})
})
